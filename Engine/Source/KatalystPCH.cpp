// The precompiled headers are used to both simplify the import of system libraries as well as precompiling them for better build and runtime performance.
// It's strongly recommended that you include the following line below when including system libraries, as it contains most of the commonly used things.
#include "KatalystPCH.h"